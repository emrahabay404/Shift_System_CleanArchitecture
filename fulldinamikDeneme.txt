<div class="col-md-2">
    <label for="pageSize">Gösterilen Kayýt Sayýsý:</label>
    <select class="form-control" id="pageSize" onchange="updatePageSize('/Home/GetShiftsWithPagination', 'pageSize', 'pageInfo', 'prevPageBtn', 'nextPageBtn', 'shiftsTable')">
        <option value="250">250</option>
    </select>
</div>

<div class="container">

    <button class="btn btn-primary" id="prevPageBtn" disabled onclick="prevPage('/Home/GetShiftsWithPagination', 'pageSize', 'pageInfo', 'prevPageBtn', 'nextPageBtn', 'shiftsTable')">Önceki</button>

    <button class="btn btn-primary" id="nextPageBtn" disabled onclick="nextPage('/Home/GetShiftsWithPagination', 'pageSize', 'pageInfo', 'prevPageBtn', 'nextPageBtn', 'shiftsTable')">Sonraki</button>

    <span id="pageInfo">Sayfa 1 / 1</span>
</div>

<table id="shiftsTable" class="table table-hover">
    <thead></thead>
    <tbody>
    </tbody>
</table>

@section Scripts {
    <script src="~/home/index.js"></script>
}

<button id="openModalButton" class="btn btn-primary">Verileri Göster</button>
--------------------------------------------------------------------------------
window.onload = function () {
    const defaultPageSize = 250;  // Varsayýlan sayfa boyutu

    // pageSize select kutusundaki varsayýlan deðeri seç
    document.getElementById('pageSize').value = defaultPageSize;

    // Ýlk veri yükleme çaðrýsýný yap
    loadShifts(
        '/Home/GetShiftsWithPagination',  // Tablo verileri için API URL'si
        'pageSize',                       // Sayfa boyutunu seçtiðiniz elementin ID'si
        'pageInfo',                       // Sayfa bilgisini göstermek için elementin ID'si
        'prevPageBtn',                    // Önceki sayfa butonunun ID'si
        'nextPageBtn',                    // Sonraki sayfa butonunun ID'si
        'shiftsTable',                    // Tablo elementinin ID'si
        '/Home/EditShift',                // Edit iþlemi için URL
        '/Home/DeleteShift',              // Delete iþlemi için URL
        true                              // Export butonlarýný tabloya ekle
    );

    populatePageSizeOptions('pageSize');  // Sayfa boyutunu dinamik olarak yükle
};


// Butona týklama ile modalý açmak
document.getElementById("openModalButton").addEventListener("click", function () {
    createAndShowModal('Todo Detayý', 'https://localhost:7161/api/Teams/paged');
});

--------------------------------------------------------------------------------